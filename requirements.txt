# --- Conexión a exchanges ---
ccxt>=4.3.0            # REST + WebSocket unificado (ccxt.pro está integrado en ccxt>=4)

# --- Análisis cuantitativo ---
pandas>=2.2.0
numpy>=1.26.0
statsmodels>=0.14.0   # Cointegración (Engle-Granger, Johansen vía VECM)
scipy>=1.12.0
pyarrow>=15.0.0       # Backend .parquet para el histórico OHLCV

# --- Backtesting ---
vectorbt>=0.26.0      # Alternativa: backtrader>=1.9.78
plotly<6              # vectorbt usa plantillas 'scattermapbox' (removidas en plotly 6+)

# --- Infraestructura ---
python-dotenv>=1.0.0  # Carga de credenciales desde .env
pydantic>=2.6.0       # Validación de configuración y modelos
uvloop>=0.19.0 ; sys_platform != "win32"   # Event loop rápido (no disponible en Windows)
tenacity>=8.2.0       # Reintentos con backoff exponencial
structlog>=24.1.0     # Logging estructurado
aiohttp>=3.9.0        # Alertas Telegram (API REST async) en el daemon
rich>=13.7.0          # Dashboard del funding radar en consola
